{
  "name": "convertra-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Convertra AudioCore — marketing & licensing site",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "astro build && wrangler pages deploy dist --project-name=convertra"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.2.1",
    "astro": "^4.16.18",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.7.3",
    "wrangler": "^4.121.0"
  }
}
